{
  "name": "simple-api-gateway",
  "version": "1.0.0",
  "description": "simple api gateway use express",
  "main": "index.js",
  "scripts": {
		"start": "pm2 start server.js --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "aka",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.1",
    "http-proxy-middleware": "^2.0.6"
  }
}
